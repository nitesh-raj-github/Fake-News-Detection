
# Fake News Detector Frontend (React + Vite + Bootstrap)

## 🚀 Setup

```bash
cd frontend
npm install
npm run dev
```

The app will start at `http://127.0.0.1:5173`.

Make sure the backend (FastAPI) is running at `http://127.0.0.1:8000`,
or change the `API_BASE_URL` in `src/api.js`.
